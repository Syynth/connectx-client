guid = require 'guid'

module.exports =
  getRandomError: (failureRate = 0.5) ->
    if Math.random() < failureRate
      {message: 'Stub!'}
    else
      null
  getNetworkDelay: ->
    500 + Math.random() * 300
  getServerId: -> "ServerId#{guid.create().value}"
