
Dispatcher = require 'connectx/dispatcher'
{ActionType} = require 'connectx/constants'

module.exports =
  updateUser: (user) -> # TODO: Add calls to remote to update other stuff!
    Dispatcher.handleClientAction
      type: ActionType.UpdateUser
      user: user
