
React = require 'react'
guid = require 'guid'

module.exports = React.createClass
  render: ->
    id = guid.create()
    <div>
      <label for={id}>File uploader</label>
      <input type="file" id={id} />
    </div>
