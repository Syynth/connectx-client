# @cjsx React.DOM

React = require 'react'
Header = require '../header'
Footer = require '../footer'
Main = require '../main'
UserHeader = require './user/header'
{EntityActionCreators} = require 'connectx/actions'
{CurrentUserStore, UserStore} = require 'connectx/stores'
{AutoStoreListener, VerifyRoutes} = require 'connectx/mixins'
{RouteHandler, State} = require 'react-router'

module.exports = React.createClass
  mixins: [State, AutoStoreListener UserStore]
  statics:
    willTransitionTo: (transition, params) ->
      new VerifyRoutes(transition, params)
        .isLoggedIn()
        .isEntityViewable params.id, 'user'
  render: ->
    data =
      current: CurrentUserStore.getCurrentActor()
      entity: UserStore.getOrDefault @getParams().id
    <div className="body_wrapper">
      <Header><UserHeader {...data} /></Header>
      <Main>
        <RouteHandler {...data} />
      </Main>
      <Footer />
    </div>
