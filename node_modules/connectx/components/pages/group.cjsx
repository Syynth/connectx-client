# @cjsx React.DOM

React = require 'react'
Header = require '../header'
Footer = require '../footer'
Main = require '../main'
{Link, RouteHandler, State} = require 'react-router'
{EntityActionCreators} = require 'connectx/actions'
{CurrentUserStore, GroupStore} = require 'connectx/stores'
{AutoStoreListener, VerifyRoutes} = require 'connectx/mixins'
SearchForm = require 'connectx/components/searchForm'
GroupHeader = require './group/header'

module.exports = React.createClass
  mixins: [State, AutoStoreListener GroupStore]
  statics:
    willTransitionTo: (transition, params) ->
      new VerifyRoutes(transition, params)
        .isLoggedIn()
        .isEntityViewable params.id, 'group'
  render: ->
    entity = GroupStore.getByIdOrDefault @getParams().id
    <div className="body_wrapper">
      <Header><GroupHeader entity={entity} /></Header>
      <Main>
        <RouteHandler current={CurrentUserStore.getCurrentActor()} entity={entity} />
      </Main>
      <Footer />
    </div>
