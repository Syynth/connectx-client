# @cjsx React.DOM

React = require 'react/addons'
_ = require 'lodash'

Icon = module.exports = React.createClass
  render: ->
    classes =
      fa: true
    _.extend classes, @props
    <i className={React.addons.classSet classes} />
