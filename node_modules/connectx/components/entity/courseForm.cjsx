# @cjsx React.DOM

React = require 'react'
AddButton = require './addButton'
{Modal} = require 'react-bootstrap'

module.exports = React.createClass
  render: -> <AddButton form={<CourseForm />}>Request a new course</AddButton>

CourseForm = React.createClass
  render: ->
    <Modal onRequestHide={@props.onRequestHide}>
      <div>haha no course for you</div>
    </Modal>
