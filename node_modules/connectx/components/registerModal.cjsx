# @cjsx React.DOM

React = require 'react'
{Modal} = require 'react-bootstrap'
{SwallowEvent, FormHelpers} = require 'connectx/mixins'
{NotificationActionCreators, SessionActionCreators} = require 'connectx/actions'

RegisterModal = module.exports = React.createClass
  mixins: [SwallowEvent, FormHelpers]
  getInitialState: -> email: '', name: '', password: '', password_confirm: ''
  formFields:
    email:
      required: true
      pattern: 'email'
      label: 'Email'
    name:
      required: true
      label: 'Name'
    password:
      required: true
      label: 'Password'
    password_confirm:
      required: true
      equalTo: 'password'
      label: 'Password Confirm'
      transient: true
  tryRegister: ->
    if @validate()
      data = @serializeForm()
      SessionActionCreators.tryRegister data
      @clearForm()
      @props.onRequestHide()
  onChange: (e) ->
    state = {}
    state[e.target.name] = e.target.value
    @setState state
  render: ->
    <Modal onRequestHide={@props.onRequestHide} title="Sign up for Golf Connectx">
      <div className="login_form_wrapper">
        <div className="partition">
          <div className="subsection">
            <form className="row1" onSubmit={@swallowEvent}>
              <label>Email *</label>
              <input ref="email" placeholder="Email" type="email" name="email" value={@state.email} onChange={@onChange} />
              <label>Full Name *</label>
              <input ref="name" placeholder="Full Name" type="text" name="name" value={@state.name} onChange={@onChange} />
              <label>Password *</label>
              <input ref="password" placeholder="Password" type="password" name="password" value={@state.password} onChange={@onChange} />
              <label>Confirm Password *</label>
              <input ref="password_confirm" placeholder="Password" type="password" name="password_confirm" value={@state.password_confirm} onChange={@onChange} />
            </form>
          </div>
        </div>
        <div className="partition center">
          <img className="form_logo" src="img/logo.png" />
          <div className="subsection">
            <p className="button register_btn modal-submit" onClick={@tryRegister}>Create Account</p>
            <p>
              <a className="modal-cancel login member">Already a member?</a>
            </p>
          </div>
        </div>
      </div>
    </Modal>
