React = require 'react'

module.exports = React.createClass
  render: ->
    <div>
      <select>
        {
          for n in [1..12]
            <option>{n}</option>
        }
      </select>
      <select>
        {
          for n in [1..60]
            <option>{n}</option>
        }
      </select>
    </div>
