module.exports = (Store) ->
  componentWillMount: ->
    Store.addChangeListener => @forceUpdate() if @isMounted()
  componentWillUnmount: ->
    Store.removeChangeListener => @forceUpdate() if @isMounted()
