module.exports = (Store) ->
  _onStoreListenerChange: -> @forceUpdate()
  componentWillMount: ->
    Store.addChangeListener @_onStoreListenerChange
  componentWillUnmount: ->
    Store.removeChangeListener @_onStoreListenerChange
