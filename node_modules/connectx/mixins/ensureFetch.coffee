
{EntityActionCreators} = require 'connectx/actions'

module.exports = (store) ->
  componentWillMount: ->
    unless store.has @props.id
      EntityActionCreators.fetchEntity @props.id, @props.type
