_ = require 'lodash'
{EventEmitter} = require 'events'

{SourceType, ActionType} = require 'connectx/config'
Dispatcher = require 'connectx/dispatcher'
Cache = require 'connectx/cache'

BaseStore = require './baseStore'

recordEntity = (action) ->
  return if action.entity.type isnt 'user'
  id = action.entity.id
  @set id, @ensureFields action.entity

class UserStore extends BaseStore
  storeKey: 'USER_STORE'
  schema:
    id: String
    type: 'user'
    email: String
    password: String
    token: String
    salt: String
    verificationId: String
    name: String
    privacy: String
    description: String
    location:
      address: String
      city: String
      state: String
      zip: String
      note: String
    profileImage: String
    coverImage: String
    images: [String]
  constructor: (dispatcher) ->
    super dispatcher
    p = new Cache @read 'cache'
  set: (key, val) -> super key, @ensureFields val
  getOrDefault: (id) -> @ensureFields @get id
  onStorageFull: -> @clearAll()
  handlers: [
    action: ActionType.UserLogin
    fn: recordEntity
  ,
    action: ActionType.EntityFetch
    fn: recordEntity
  ]

module.exports = new UserStore Dispatcher
